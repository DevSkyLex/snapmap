<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Base application theme.
      MaterialComponents (Bridge) requis par le PaymentSheet Stripe
      (@capacitor-community/stripe) : sans ça, l'ouverture du paiement crashe.
      Les variantes "Bridge" restent compatibles avec l'usage AppCompat de Capacitor.
    -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- Thème appliqué après le splash : descend de MaterialComponents (cf. Stripe). -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
